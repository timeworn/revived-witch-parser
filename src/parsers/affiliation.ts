import { Localizer } from "src/utils/localizer";

export class AffiliationParser {
  private localizer: Localizer;

  constructor(localizer: Localizer) {
    this.localizer = localizer;
  }

  parse() {}

  transform() {}
}
